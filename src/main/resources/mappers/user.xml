<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunjae.project5.persis.UserMapper">
    <resultMap id="User" type="com.chunjae.project5.entity.User">
        <result property="id" column="user_id" />
        <result property="active" column="active" />
        <result property="loginId" column="login_id" />
        <result property="userName" column="user_name" />
        <result property="password" column="password" />
    </resultMap>
    <select id="findUserByLoginId" resultMap="User">
        select user_id, active, login_id, user_name, password
        from user
        <where>
            <if test=" loginId != null and loginId != '' ">
                and login_id = #{loginId}
            </if>
        </where>
    </select>

    <select id="getLastInsertUser" resultMap="User">
        select *
        from user
        order by user_id desc limit 1
    </select>

    <insert id="setUserInfo" parameterType="com.chunjae.project5.entity.User" useGeneratedKeys="true">
        insert into user (active, login_id, user_name, password)
        values (#{param.active}, #{param.loginId}, #{param.userName}, #{param.password})
    </insert>

    <update id="addPt">
        update user set pt=pt+5 where login_id=#{loginId} and pt &lt; 100
    </update>

    <update id="minusPt">
        update user set pt=pt-5 where login_id=#{loginId} and pt &gt; 0
    </update>

</mapper>